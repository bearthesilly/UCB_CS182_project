# UCB_CS182_project
The repository for the project of UCB CS182 deeplearning.

First execute `setup.sh` to install everything (conda environment). `setup.sh` will create a conda environment called llm which will have everything installed. 

and then execute `cs182_proj_python.py`
Reproduction is within the file of `cs182_proj_python.py`. Execute the file.
